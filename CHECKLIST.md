# CHECKLIST — 公開・本番化前の確認事項

> このファイルは、ホテル側の確認・素材提供が必要な事項と、公開前の設定手順をまとめたものです。
> 表示・動作に影響する「創作してはいけない未確認情報」は、サイト上で「要相談／詳細はお問い合わせください」と表記しています。

## 1. ホテル側に確認が必要な情報（未確認のためサイトでは「要相談」表記）

- [ ] ホテル全体の**最大収容人数**（全65室・最大10名/室は記載済。総定員の断定は未記載）
- [ ] **O2ドクター 酸素カプセル**の利用料金・利用時間・利用条件
- [ ] **MaxHub**の利用料金・予約条件
- [ ] **打ち上げ花火**の料金・実施可能期間・申込期限（※要事前相談・天候等で不可の旨は記載済）
- [ ] 旅行会社向けの**手数料・精算条件**
- [ ] **仮押さえ**の期限
- [ ] **キャンセル規定**
- [ ] 楽器の**練習可能時間**（音楽合宿）
- [ ] **アレルギー対応**の範囲
- [ ] 各**宿泊プランの正式料金**（現在は「目安」表記。要確定）

## 2. 差し替えが必要な素材

- [ ] **お客様の声**（実績セクション）＝現在「サンプル」表記。実際のお声＋掲載団体名の**掲載許諾**を取得のうえ差し替え
- [ ] **AI生成イメージ画像**（花火・合宿・会議室・スポーツ分析・Web会議・吹奏楽）＝実写真が用意でき次第差し替え。差し替えるまでは「写真はイメージ」である旨をホテルと共有

## 3. 団体問い合わせフォームの送信先設定（公開前に必須）

現状：`contact.html` のフォームは**送信先が未設定**のため、送信時に「お電話でのご相談」へ正直にご案内します（偽の送信成功は表示しません）。

**設定手順（どちらか）：**
- **A. Formspree（無料枠あり・最短）**
  1. https://formspree.io で登録し、フォームIDを取得（例 `https://formspree.io/f/abcdwxyz`）
  2. `contact.html` 内の `var ENDPOINT = "";` を `var ENDPOINT = "https://formspree.io/f/abcdwxyz";` に変更
  3. 通知先メールをFormspree側で設定（HTMLにメールは書かない）
- **B. Google Apps Script（無料・自前）**
  1. スプレッドシート＋GASでWebアプリ（doPost）を作成し公開URLを取得
  2. 同じく `ENDPOINT` にそのURLを設定

> 送信先URL・管理者メールは**HTMLに直書きしない**方針。ENDPOINTの1行変更だけで有効化できます。

## 4. アクセス解析（GA4）の設定

現状：計測イベントは `window.dataLayer` に積むだけで、GA4は未接続（架空IDは入れていません）。

**有効化手順：**
1. GA4の測定ID（`G-XXXXXXX`）を取得
2. 各HTMLの `</head>` 直前に GA4 の gtag スニペットを1つ追加（測定IDを差し込む）
3. `assets/site.js` の `dataLayer.push({event:...})` がそのままイベントとして送られます
   - 計測イベント名：`tel_click` / `inquiry_cta_click` / `inquiry_form_start` / `inquiry_form_submit`
   - （個人情報・フォーム入力値は送信しません）

## 5. 検索エンジン公開（インデックス）への切替

現状：`robots.txt` で全ページのインデックスを抑止（ドラフトのため）。
本番化（実データ差し替え後）：`robots.txt` の `Disallow: /` を `Allow: /` に変更。`sitemap.xml` は設定済み。

## 6. 次フェーズの残タスク（今回未実施）

- [x] **ターゲット別ページ**：sports / music / training / touring / travel-agent（旅行会社向け）＝作成済み・トップに「目的で選ぶ」入口も設置。内容の事実確認（未確認項目は各ページで「要相談」表記）
- [ ] **画像の最適化**：1MB超のPNG8枚をWebP化＋`srcset`（表示速度・LCP改善）
- [ ] **モデルスケジュール**（スポーツ/研修/音楽/ツーリング）のタブ表示
- [ ] Lighthouse実測（Performance / Accessibility / SEO）
